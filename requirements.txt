# ============================================================
# 化工安全生产合规智能体 —— 全量依赖
# 说明：langchain-core 为 langgraph 的底层依赖自动引入，
#       本项目仅使用 LangGraph 状态图，不使用旧版 Chain API。
# ============================================================

# ---------- 核心 AI 编排 ----------
langgraph>=0.2.60
langchain-core>=0.3.15

# ---------- 大模型（OpenAI 兼容：OpenAI / Azure / vLLM / Ollama） ----------
openai>=1.40.0

# ---------- 向量检索 & Embedding ----------
faiss-cpu>=1.8.0
sentence-transformers>=3.0.1
numpy>=1.26.4

# ---------- 图数据库 ----------
neo4j>=5.20.0

# ---------- CV 目标检测（YOLOv8） ----------
ultralytics>=8.2.0
opencv-python>=4.9.0.80

# ---------- API 服务 ----------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ---------- 前端演示 ----------
streamlit>=1.36.0
requests>=2.32.0

# ---------- 数据采集 ----------
beautifulsoup4>=4.12.3
lxml>=5.2.0
pypdf>=4.0.0

# ---------- 工具 ----------
python-dotenv>=1.0.1
